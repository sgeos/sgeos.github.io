# jekyll
_site
.sass-cache
.jekyll-metadata
.jekyll-cache

# bundler (local gem install path and config, if a local bundle is ever used)
vendor/
.bundle/

# vim
*~
*.swp
*.swo

# drafts not worth commiting
hidden.*.markdown

# Project-local temporary files
secret/*
tmp/*

# Whitelist .gitkeep files globally.
!.gitkeep
