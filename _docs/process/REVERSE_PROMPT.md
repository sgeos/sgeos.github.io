## Last Updated

**Date**: 2026-08-31
**Task**: **A342 publication review, the fourth and last of four passes.** Committed and **pushed**.
**Not published**, and publication of the series still not authorised.

---

## The Survey Contradicted Itself and Every Automated Check Passed

**The paragraph that interprets the survey was stale in all six of its statistics**, and the Source
Base four hundred lines below it stated the corrected values. The article said the autonomy clusters
held 1,771 records and it said 2,558 was the total of the same four clusters.

**The primary pass regenerated every mechanical part of the survey and left the hand-written
interpretation alone.** Cluster rows, citation runs and the total were all correct and are
regenerated by a script. The paragraph a person wrote about them was not.

**The number verifier confirmed the stale figure.** It checked that string with `present()`, which
asks whether the article still says what it used to say. **That check goes green precisely when a
number goes stale**, since a stale number is by definition still present. It is now recorded in
`VERIFICATION_TRAPS.md` along with the repair, which is a separate checker that extracts every stated
statistic from the finished article and recomputes it from the reference data. **It found six defects
on its first run and reports zero now**, across cluster rows, survey shape, the source base chain and
the results probe.

---

## The Probe That This Pass Exists To Run

**A340 and A341 both found the survey under-covering the subjects of the article's own conclusions**,
so eight claims were taken from the closing sections and measured in the vocabulary of the field each
belongs to. **Seven of the eight measured thin.**

| result-driven subject | before | after |
|---|---|---|
| lift to drag ratio of a tailless aeroplane | 16 | **201** |
| flight test methodology behind the software-block claim | 32 | **87** |
| queueing and operator utilisation | 49 | 55 |
| individual differences and the operator on a day | 18 | 27 |
| the value of autonomy | 24 | 26 |
| coupled and heterogeneous tasks | 4 | 17 |
| reported operator-to-vehicle ratios | 9 | 11 |

**Two closed and five did not, and the five did not fail for want of effort.**

---

## The Fourth Kind Of Thin, Appearing Again In A New Guise

**A harvest aimed at how many vehicles one operator can hold returned 1,446 records and almost none
of them were about it.** The words that name the subject belong to a literature about photogrammetry,
path planning, crop estimation and counter-drone radar that is larger by two orders of magnitude.
**The vocabulary does not discriminate**, which is A341's designation lesson in a different field and
cannot be repaired by harvesting harder.

**The sharpest consequence sits under the article's most exposed claim.** The assertion that a ratio
of two has not been beaten in twenty years rests on eleven records, **and four of those eleven are the
organisational span of control**, which asks how many subordinates a commander may hold. That is the
ancestor of the article's question and not an instance of it. **They were kept and named rather than
quietly removed**, and a fifth entry was added to Where the Framing Breaks Down saying plainly that a
bibliographic survey is a poor instrument for a negative claim.

---

## Two Library Defects, Both Fixed Where They Stop Recurring

**`refs.display` lowered all-capitals AUTHOR names and left its no-author branch shouting.** That
branch falls back to the first words of the title, so a publisher shouting its own title produced
shouted link text. **The 2026-08-14 audit fixed 3,564 of these by hand across 26 articles and A342
then harvested four more**, which is the definition of a defect that belongs in the library.
`refs.decap` now normalises a predominantly uppercase title while preserving initialisms, deciding on
the whole string first because `IFAC` and `ON` are the same length. **Tests are 83 of 83.**

**The sweep store gained six families**, five of them consequences of the word unmanned and one of
them the semiconductor packaging sense of fan-out. **A343 through A345 are unmanned combat aircraft
resting on the same keystone and will meet all six.**

---

## Three Findings About The Method Itself

**A pattern added to catch a homonym created one.** The packaging family named `fan-in`, and
`Fan-in-Wing` is a lift-fan propulsion configuration, so it threw out a blended-wing-body paper that
is exactly on subject. The term was unnecessary and was removed.

**A prune keyed by anchor and a merge keyed by URL disagreed about what a record is**, so three
records were removed and re-added inside one pass and the pool depended on the order the steps ran
in. Reconciled by URL the arithmetic closes exactly, at 9,444 minus 50 plus 2,038 equals 11,432.

**Removing gate escapes was allowed here and is normally refused.** The 2026-08-14 audit declined to
strip them because a survey states its own counts in prose. **That objection does not apply inside a
pass that regenerates the survey and every count in it**, so fifty were removed in the one window
where removal is safe, with the anchors the argument cites protected by name.

---

## State

**A342 is committed and pushed. All four passes complete.** 23,670 lines, **20 display equations**
held and measured, **11,507 reference definitions**, 128,143 words of which 10,763 are author prose.

`_verify.py` **zero errors and zero warnings**, `lint.py` zero defects and zero conventions,
reference integrity zero undefined, zero orphaned, zero duplicate URLs. `_lib` tests **83 of 83**.
Every survey statistic recomputed from the data and every stated number checked. **Zero
caps-emphasis spans**, which is the first pass across A341 and A342 where I did not introduce one.

**The stub-isolated production build succeeded in 8,019 seconds with no Liquid error**, and it was
restarted against the exact final bytes rather than against a copy taken seven prose lines earlier.
**The rendered audit reports no findings across 58 pages.** Source and rendered display-equation
counts agree at **20**, being 20 opening and 20 closing rendered delimiters with **zero raw dollar
pairs leaking**, which is the check A341 had to invent because a display equation demoted to inline
math is invisible to the audit. **Zero unresolved reference brackets in the rendered HTML**, all
14 sections render in order, and each of the five citations added this pass resolves to its link.

**Forty-six of seventy-two drafted, none published, publication never authorised.**

---

## Next

**A343, Boeing X-46**, editorial date 2025-11-21, series index 47. **The X-46 was cancelled before an
airframe was built**, so expect the documentation-poor or anomaly class and decide from the record.

**Two items remain yours**, being whether `math-display-inlined` should move from `lint.py` to
`_verify.py`, now with two incidents behind it, and the caps defect on the live
`_posts/2026-08-06-native_lowering_coverage.markdown`.
